import { WarehouseStateChanged } from './warehouse-state-changed';

describe('WarehouseStateChanged', () => {
  it('should create an instance', () => {
    expect(new WarehouseStateChanged()).toBeTruthy();
  });
});
